# Legacy Environment Archive

This directory is not part of the active environment flow.
Use only `env/dev/` and `env/prod/` for current operations.
Archived `lab` files remain here only for historical reference and migration support.
