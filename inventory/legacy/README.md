# Legacy Inventory Archive

This directory is not part of active inventory discovery.
Only `inventory/dev/` and `inventory/prod/` are active environments.
Archived `lab` inventory remains here only for historical reference and migration support.
